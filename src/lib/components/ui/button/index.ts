import Root from "./button.svelte";
import type { ButtonProps } from "./button.svelte";
import {
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from "./utils";

export {
	Root,
	type ButtonProps as Props,
	//
	Root as Button,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};
